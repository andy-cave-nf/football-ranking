describe('given a single match between two teams from a json source when the match is processed', () => {
  it.todo('stores both teams with updated ratings')
  it.todo('writes a results file with teams sorted by rating')
})
